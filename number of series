#include <stdio.h>

int main() {
    int N, i;

    printf("Enter the value of N: ");
    scanf("%d", &N);

    printf("Output = {");
    for (i = 1; i <= N; i++) {
        printf("%d", i);
        if (i < N) {
            printf(",");
        }
    }
    printf("}\n");

    return 0;
}
